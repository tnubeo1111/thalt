text = "Pythonn is a programming language"
length = len(text)
print(f'The length of the string is: {length}')


# Giải thích chi tiết:
# 1. Biến `text` chứa một chuỗi ký tự: "Pythonn is a programming language".
# 2. Hàm `len()` được sử dụng để tính số lượng ký tự trong chuỗi `text`, bao gồm cả khoảng trắng.
#    Kết quả được lưu vào biến `length`.
# 3. Hàm `print()` sử dụng f-string để in ra kết quả dưới dạng: 
#    "The length of the string is: {length}", trong đó `{length}` là giá trị của biến `length`.
